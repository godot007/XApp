//
//  CommunityView.swift
//  XApp
//
//  Created by Roy on 12/6/2025.
//

import SwiftUI

struct CommunityView: View {
  var body: some View {
    VStack {}
      .frame(maxWidth: .infinity, maxHeight: .infinity)
      .background(
        LinearGradient(
          colors: [.blue, .orange], startPoint: .topLeading,
          endPoint: .bottomTrailing
        )
      )
      .ignoresSafeArea()
  }
}
